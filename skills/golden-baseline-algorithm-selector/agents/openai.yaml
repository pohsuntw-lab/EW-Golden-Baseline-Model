interface:
  display_name: "Algorithm Selector／算法選擇"
  short_description: "Select only executable baseline algorithms／只選可執行算法"
  brand_color: "#54405C"
  default_prompt: "Use $golden-baseline-algorithm-selector to create a process policy using only backend-confirmed executable algorithms.／請只使用後端確認可執行的算法建立工藝政策。"
policy:
  allow_implicit_invocation: true
